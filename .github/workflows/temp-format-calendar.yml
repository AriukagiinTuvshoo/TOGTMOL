name: Temporary format calendar feature
on:
  push:
    branches: [feat/calendar-planning-v6]
jobs:
  format:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/calendar-planning-v6
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run format
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No formatting changes."
            exit 0
          fi
          git commit -m "chore: format calendar feature branch"
          git push origin feat/calendar-planning-v6
      - name: Remove temporary workflow
        run: |
          rm -f .github/workflows/temp-format-calendar.yml
          git add .github/workflows/temp-format-calendar.yml
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "chore: remove temporary format workflow"
          git push origin feat/calendar-planning-v6
